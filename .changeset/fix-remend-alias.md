---
"@lofcz/streamdown": patch
---

Fix the published `remend` dependency to use an npm alias instead of a `workspace:` spec so consumers can install `@lofcz/streamdown` without extra package-manager overrides.
