---
"@lofcz/streamdown": patch
---

Add the OpenSCAD viewer's default `h-[400px]` to the Tailwind source class list so consumer apps generate the utility (the viewer container would otherwise collapse to zero height).
