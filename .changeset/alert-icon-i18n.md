---
"@lofcz/streamdown": minor
---

GFM alerts: render octicons as inline SVG (fix sanitize stripping `d`/`width`/`height`/`aria-hidden` so icons no longer need octicon CSS), apply per-kind colored border/background/title accents, and localize the alert title (Note/Tip/Important/Warning/Caution) via the `translations` prop.
