---
"@lofcz/streamdown": patch
---

GFM alerts: render the warning icon as an outlined triangle (split evenodd path instead of a solid fill) and give every alert icon `fill="currentColor"` so it matches its per-kind title color.
